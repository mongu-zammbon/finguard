services:
  - type: web
    name: finguard
    runtime: python
    plan: free
    buildCommand: python3 -m compileall -q app
    startCommand: python3 -m app.server --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthz
